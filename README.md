# guessing_game1to10000
python practice
#Guessing Game (1–1000)

A simple Python guessing game that picks a random number between 1 and 1000.  
You keep guessing until you find the correct number.

## Features
- Uses `random.randint()` to generate numbers  
- Handles invalid input (non-numeric guesses)  
- Demonstrates `while` loops, `if` statements, and user input

## Run the program
```bash
python guessing_game.py
